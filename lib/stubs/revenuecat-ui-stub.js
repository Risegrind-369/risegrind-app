/**
 * Web stub for react-native-purchases-ui (RevenueCat UI)
 * RevenueCat UI is a native-only SDK. This stub prevents Metro bundler errors on web.
 */
const RevenueCatUI = {
  presentPaywallIfNeeded: () => Promise.resolve('NOT_PRESENTED'),
  presentPaywall: () => Promise.resolve('NOT_PRESENTED'),
};

module.exports = RevenueCatUI;
module.exports.default = RevenueCatUI;
